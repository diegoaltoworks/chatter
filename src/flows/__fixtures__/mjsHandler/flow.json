{
  "id": "mjsHandler",
  "name": "MJS Handler",
  "description": "Uses a handler.mjs instead of handler.ts or handler.js",
  "triggerKeywords": ["mjs"],
  "schema": {
    "type": "object",
    "properties": { "x": { "type": "string" } },
    "required": ["x"]
  }
}
