{
  "id": "jsHandler",
  "name": "JS Handler",
  "description": "Uses a handler.js instead of handler.ts",
  "triggerKeywords": ["js"],
  "schema": {
    "type": "object",
    "properties": { "x": { "type": "string" } },
    "required": ["x"]
  }
}
