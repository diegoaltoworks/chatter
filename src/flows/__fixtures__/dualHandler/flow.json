{
  "id": "dualHandler",
  "name": "Dual Handler",
  "description": "Ships both handler.ts and handler.js to test extension precedence",
  "triggerKeywords": ["dual"],
  "schema": {
    "type": "object",
    "properties": { "x": { "type": "string" } },
    "required": ["x"]
  }
}
